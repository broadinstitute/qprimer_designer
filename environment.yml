name: qprimer-designer
channels:
  - pytorch
  - bioconda
  - conda-forge
dependencies:
  - python=3.12
  # Bioinformatics tools
  - viennarna>=2.6
  - bowtie2>=2.5
  - sam2pairwise
  - mafft>=7.5
  # Workflow
  - snakemake>=9.0
  # ML dependencies (moved from pip for better Docker layer caching)
  - pytorch>=2.0
  - biopython>=1.84
  - pandas>=2.0
  - numpy>=1.24
  - scikit-learn>=1.3
  - scipy>=1.11
  - joblib>=1.3
  - einops>=0.7
  # For pip install
  - pip
